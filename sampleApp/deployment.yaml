apiVersion: apps/v1
kind: Deployment
metadata:
  name: sampleapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sampleapp
  template:
    metadata:
      labels:
        app: sampleapp
    spec:
      containers:
      - name: sampleapp
        image: dockeritc778/sampleapp:20260625.12
        ports:
        - containerPort: 5000

---

apiVersion: v1
kind: Service
metadata:
  name: sampleapp-svc
spec:
  type: ClusterIP
  selector:
    app: sampleapp
  ports:
    - port: 80      
      targetPort: 5000
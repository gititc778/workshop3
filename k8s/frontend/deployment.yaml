apiVersion: apps/v1
kind: Deployment
metadata:
  name: hospital-frontend
  labels:
    app: hospital-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hospital-frontend
  template:
    metadata:
      labels:
        app: hospital-frontend
    spec:
      containers:
        - name: hospital-frontend
          image: dockeritc778/hpm:v1
          ports:
            - containerPort: 80

apiVersion: v1
kind: Service
metadata:
  name: hospital-frontend
  labels:
    app: hospital-frontend
spec:
  type: ClusterIP
  selector:
    app: hospital-frontend
  ports:
    - port: 80
      targetPort: 80